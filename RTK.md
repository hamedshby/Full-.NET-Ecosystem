# قوانین دائمی پروژه آکادمی دات‌نت

## روش اجرا

- همهٔ طراحی‌ها، پیاده‌سازی‌ها و تغییرات با best practiceهای معتبر و مرتبط انجام شوند؛ از جمله دسترس‌پذیری، امنیت، نگهداشت‌پذیری، Responsive Design، Performance و آزمون‌پذیری. انتخاب دقیق موارد لازم متناسب با ماهیت هر تغییر و با قضاوت فنی انجام شود.
- تمام برنامه‌ها و تغییرات به‌صورت پیش‌فرض با روش «اجرا در همین جلسه» انجام شوند.
- از Subagent یا اجرای چندعاملی استفاده نشود، مگر اینکه کاربر صریحاً همان روش را درخواست کند.
- پس از تأیید طرح، کار تا پیاده‌سازی و Verification در همین جلسه ادامه پیدا کند.
- پس از روشن‌شدن هدف، برای تصمیم‌های فنی و کم‌ریسک تأیید تکراری درخواست نشود. بهترین گزینه با قضاوت فنی انتخاب و اجرا شود.
- فقط وقتی سؤال پرسیده شود که مانع واقعی، تصمیم پرریسک، اطلاعات کشف‌ناپذیر یا تغییر اساسی محدوده وجود دارد.
- برای تغییرات محدود، تأیید طرح و نوشتن آزمون الزامی نیست؛ تغییر مستقیماً اجرا و با Verification متناسب بررسی شود.

## کیفیت محتوای آموزشی

- همهٔ توضیحات باید شفاف، مرحله‌به‌مرحله و برای یک Junior Developer قابل‌فهم باشند. هر مفهوم از یک مثال ساده یا رفتار قابل مشاهده آغاز شود، اصطلاحات پیش از استفاده توضیح داده شوند و سپس جزئیات عمیق‌تر ارائه شوند.
- پاسخ از مثال ساده و رفتار قابل مشاهده شروع شود و سپس به جزئیات عمیق برسد.
- هیچ اصطلاحی پیش از تعریف‌شدن، پایهٔ توضیح قرار نگیرد.
- هر ادعای اصلی با مثال، استدلال یا توضیح رفتار پشتیبانی شود.
- کد و نتیجه یا رفتار آن هر دو توضیح داده شوند.
- بخش‌های یک پاسخ نباید متن یکسان یا بازنویسی سطحی یکدیگر باشند.
- نکات کلیدی از پاراگراف دیگری به‌صورت ماشینی استخراج نشوند؛ برای هر سؤال مستقل نوشته شوند.
- کیفیت با تعداد کلمات سنجیده نشود؛ وضوح مدل ذهنی، پاسخ به «چرا» و کاربرد عملی معیار است.
- پاسخ‌های مقدماتی از مفاهیم پیشرفته آغاز نشوند. جزئیات Compiler، CLR، Stack، Heap و Performance پس از توضیح مبانی بیایند.
- ادعاهای فنی ظریف با مستندات رسمی Microsoft Learn، .NET documentation یا C# language specification بررسی شوند.

## قانون اصطلاحات فنی

اصطلاحات رسمی .NET و C# ترجمه نمی‌شوند. متن توضیحی فارسی است، اما نام رسمی هر concept، keyword، type، API، runtime component، language feature، pattern و design principle با شکل متعارف انگلیسی باقی می‌ماند.

نمونهٔ درست:

> `Boxing` فرایندی است که طی آن یک `Value Type` در قالب `object` قرار می‌گیرد.

نمونهٔ نادرست:

> جعبه‌سازی فرایندی است که طی آن یک نوع مقداری در قالب شیء قرار می‌گیرد.
